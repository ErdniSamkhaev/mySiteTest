---
const { title, url, tags = [] } = Astro.props;
---
<li>
  <a href={url}>{title}</a>
  {tags.length > 0 && (
    <div class="tags">
      {tags.map((tag: string) => (
        <p class="tag">
          <a href={`/tags/${tag}`}>{tag}</a>
        </p>
      ))}
    </div>
  )}
</li>
