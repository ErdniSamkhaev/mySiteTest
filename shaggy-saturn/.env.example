# Supabase — для лайков постов. Скопируй в .env и подставь значения
# из дашборда: Project Settings → API.
# anon-ключ публичный (попадает в браузер), данные защищены RLS — это нормально.
PUBLIC_SUPABASE_URL="https://ВАШ-ПРОЕКТ.supabase.co"
PUBLIC_SUPABASE_ANON_KEY="ВАШ-ANON-КЛЮЧ"
